SUPERGOAL_PHASE_START
Phase: 3 of 16 — Simplify dependency surface
Task: Reduce wallet and Web3 dependencies to the packages the app uses and eliminate critical/high production advisories.
Type: dependencies, supply-chain, bundle, cleanup
Mandatory commands: cd web && npm run lint, cd web && npm run typecheck, cd web && npm test, cd web && npm run test:coverage, cd web && npm run test:e2e, cd web && npm run build, cd web && npm ls --omit=dev, cd web && npm audit --omit=dev --audit-level=high
Acceptance criteria: 11
Evidence required: before-and-after dependency inventory, wallet smoke evidence, audit report, npm tree result, build warning comparison, full quality-gate exit codes
Depends on phases: 1, 2

## Why

The current wallet bundle pulls unused adapters and a large vulnerable dependency graph.

## Work

- Replace broad wallet adapter imports with only Phantom and Solflare packages used by the UI.
- Trace each production dependency to maintained imports and record whether it is kept, replaced or removed.
- Remove `ethers` and other unused production dependencies after verifying no imports remain.
- Keep Solana Web3.js and wallet connection behavior intact for supported wallets.
- Eliminate every reachable production critical and high advisory.
- Remove obsolete root scripts or clearly keep them out of the production app path.
- Preserve a clean lockfile generated by npm.

## Acceptance criteria (all must pass — verify each in transcript)

- Every production dependency has a maintained runtime import or a written build-time reason naming its source path.
- The app imports Phantom and Solflare adapters directly and neither imports the broad wallet bundle nor initializes any unlisted adapter.
- `ethers` is absent from the manifest, lockfile and maintained imports unless the inventory proves an active production call site.
- `npm ls --omit=dev` exits 0 with no invalid, missing or extraneous production package.
- `npm audit --omit=dev --audit-level=high` exits 0 with zero critical and zero high production advisories.
- A provider test asserts that the configured wallet list contains exactly the two supported adapters once each.
- Desktop and mobile E2E tests reach the connect-wallet UI and list both supported wallets without browser errors.
- The build emits none of the prior WalletConnect dynamic-dependency, missing `pino-pretty` or localstorage-file warnings.
- `web/package-lock.json` matches the manifest, and a fresh `npm ci` requires no rewrite.
- Home-route client JavaScript does not exceed the phase-2 baseline, with both totals recorded.
- All mandatory quality, E2E, dependency-tree and high-severity audit commands exit with code 0.

## Mandatory commands (run each, surface last ~10 lines + exit code)

- `cd web && npm run lint`
- `cd web && npm run typecheck`
- `cd web && npm test`
- `cd web && npm run test:coverage`
- `cd web && npm run test:e2e`
- `cd web && npm run build`
- `cd web && npm ls --omit=dev`
- `cd web && npm audit --omit=dev --audit-level=high`

## Evidence required in transcript

- Show before-and-after production dependency counts and the keep, replace or remove inventory.
- Show direct adapter imports and zero search results for the broad bundle.
- Show wallet-provider tests and desktop plus mobile smoke results.
- Show build warnings and home-route JavaScript totals before and after.
- Show the audit severity counts, `npm ls --omit=dev` result and every mandatory exit code.

## Notes

Do not force a major framework or Solana migration solely to chase low or moderate findings. Any remaining advisory must be below high severity and have its dependency path plus removal condition recorded.

---

The agent will, during execution, print SUPERGOAL_PHASE_START (above),
do the work, then print SUPERGOAL_PHASE_VERIFY, MEMORY_SAVED, and
SUPERGOAL_PHASE_DONE in order. On failure, the agent follows the
3-strike recovery protocol in .supergoal/production-hardening-king-of-the-screen-T6f8XH/PROTOCOL.md without further
instruction needed here.
